# Outputs

Pasta reservada para entregáveis consolidados da agência.

Os artefactos de execução devem viver preferencialmente dentro de `clients/<cliente>/runs/<run_id>/artifacts/`.

Esta pasta pode ser usada para:

- entregas finais
- propostas
- pacotes exportados

services:
  tela88-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tela88-web
    env_file:
      - .env.local
    environment:
      NODE_ENV: production
      PORT: 3000
      HOSTNAME: 0.0.0.0
    ports:
      - "3000:3000"
    restart: unless-stopped

import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Consultoria Estratégica Gratuita — Tela 88",
  description:
    "30 minutos com um especialista da Tela 88. Falamos sobre visibilidade, tráfego, conversão e retenção — sem compromissos.",
};

export default function ContactosLayout({ children }: { children: React.ReactNode }) {
  return children;
}
